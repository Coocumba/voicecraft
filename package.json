{
  "name": "voicecraft-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter @voicecraft/web dev",
    "build": "pnpm --filter @voicecraft/web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  }
}
