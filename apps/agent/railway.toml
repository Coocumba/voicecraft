[build]
builder = "dockerfile"
dockerfilePath = "apps/agent/Dockerfile"
# Root Directory: "/" (repo root) — same as web service

[deploy]
startCommand = "python -m src.agent.worker start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
