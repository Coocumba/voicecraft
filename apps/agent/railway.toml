[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# In Railway dashboard: set Root Directory to "apps/agent"

[deploy]
startCommand = "python -m src.agent.worker start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
